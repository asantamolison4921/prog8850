name: Assignment 2

on:
  push:
    branches:
      - main

jobs:
  echo-secret:
    runs-on: ubuntu-latest
    
    environment: prog8850-assignment2

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Echo DB_USERNAME
        run: echo ${{ secrets.DB_USERNAME }}
